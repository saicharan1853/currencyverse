# Environment Variables
NODE_ENV=development
PORT=4000
CORS_ORIGIN=http://localhost:3000
JWT_SECRET=your_jwt_secret_key_here
SESSION_SECRET=your_session_secret_key_here

# API Keys (for external currency API if needed)
#CURRENCY_API_KEY= "fca_live_KXrv6uD2UFSDSAhIFHzE7Bn6ohE3eW9q7wBdSLcF"
#EXTERNAL_CURRENCY_API_URL= "https://api.freecurrencyapi.com/v1/latest"

# Database Configuration
# For local development without MongoDB, we'll use a fallback
# Replace <db_password> with your actual MongoDB password
#DATABASE_URL=mongodb+srv://cverse:yourpassword@cluster0.qbsbxnl.mongodb.net/currencyverse?retryWrites=true&w=majority
# For local MongoDB Compass, use: 
DATABASE_URL=mongodb://localhost:27017/currencyverse

# Fallback mode when MongoDB is not available
USE_MEMORY_DB=false

# JWT Secret (for authentication)
# JWT_SECRET=your_jwt_secret_here
